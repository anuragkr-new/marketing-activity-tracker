# Single Railway service: install deps, build client, run Express (API + static SPA).

[phases.install]
cmds = ['npm ci']

[phases.build]
dependsOn = ['install']
cmds = ['npm run build']

[start]
cmd = 'npm start'
